{% extends 'layout' %}

{% block content %}

{% import 'forms' as forms %}

{{ include('include') }}

<p>{{ forms.input('username') }}</p>
<p>{{ forms.input('password', null, 'password') }}</p>
    {%- for entry in posts -%}
        <h2>{{ entry.title }}</h2>
        <p>{{ entry.body }}</p>
    {% endfor %}
{% endblock %}
