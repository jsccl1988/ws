{% import 'form-bs3' as forms %}
 

{{ forms.start(form, { method: 'GET', attrs: { autocomplete: 'on'} }) }}
{{ forms.input(form.fields.username, {id: "username", label: "label"}) }}
{{ forms.end(form) }}
